import Ember from 'ember';
import AnimationMixin from '../../mixins/animation';

var DemosDerivativesController = Ember.Controller.extend(Ember.Evented, AnimationMixin, {
  
});

export default DemosDerivativesController;