import Ember from 'ember';
import AnimationMixin from '../../mixins/animation';

var DemosRevolutionsController = Ember.Controller.extend(Ember.Evented, AnimationMixin, {
  
});

export default DemosRevolutionsController;