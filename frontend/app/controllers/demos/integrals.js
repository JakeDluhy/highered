import Ember from 'ember';
import AnimationMixin from '../../mixins/animation';

var DemosIntegralsController = Ember.Controller.extend(Ember.Evented, AnimationMixin, {
  
});

export default DemosIntegralsController;